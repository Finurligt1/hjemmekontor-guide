---
import { siteConfig } from "../site.config";
import { withBase } from "../lib/url";
---

<header class="site-header">
  <div class="wrapper">
    <a class="site-logo" href={withBase("/")}>{siteConfig.name}</a>
    <nav class="site-nav">
      <a href={withBase("/")}>Forside</a>
      <a href={withBase("/om/")}>Om os</a>
      <a href={withBase("/affiliate-oplysning/")}>Affiliate-oplysning</a>
    </nav>
  </div>
</header>
